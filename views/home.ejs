<!DOCTYPE html>
<html lang="en" data-theme="emerald">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <title>Why It Broke</title>
</head>
<body tabindex="-1">
    <%- include('partials/navbar', {page: '/home'}) %>
    
    <div class="container mx-auto">
        <div class="hero min-h-[400px] bg-[url('/images/banner.jpg')] bg-cover bg-center rounded-lg p-6 mb-5">
            <div class="hero-overlay bg-black bg-opacity-40 rounded-lg"></div>
            <div class="hero-content text-center text-neutral-content">
              <div class="max-w-md">
                <h1 class="text-5xl font-bold">Why It Broke</h1>
                <p class="py-6">Real bugs, Real fixes. Built by devs.</p>
                <a href="/api/posts" class="btn btn-primary">View Posts</a>
              </div>
            </div>
        </div>

        <h2 class="text-xl font-bold mb-4">Recent Posts</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% recentPosts.forEach(post => { %>
            <div>
              <div class="card w-full bg-base-100 card-lg shadow-xl h-full">
                <div class="card-body">
                  <h2 class="card-title line-clamp-2 min-h-[3.6rem]"><%= post.title %></h2>
                  <p class="line-clamp-2 leading-snug mb-8 text-sm"><%= post.description %></p>
                  <div class="flex justify-between items-center mt-auto">
                    <div class="text-xs text-gray-500 px-2 py-1 rounded font-medium"><%= post.language.name %></div>
                    <a href="/api/posts/<%= post._id %>" class="btn btn-primary btn-sm">Debug</a>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
    </div>
</body>
</html>